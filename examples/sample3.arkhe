<FONS>
</FONS>

<INTRODUCTIO>
</INTRODUCTIO>

<DOCTRINA>
FCON subjecto: nihil () -> {
    VCON s:filum = accipere() <- ("name? ");
    indicant() <- (s);
    VCON n:filum = accipere() <- ("number? ");
    indicant() <- (n*2);
    VCON conv_n:inte = inte() <- (n);
    indicant() <- (conv_n*2);
};
</DOCTRINA>
