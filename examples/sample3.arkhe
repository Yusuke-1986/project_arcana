<FONS>
</FONS>

<INTRODUCTIO>
</INTRODUCTIO>

<DOCTRINA>
FCON subjecto: nihil () -> {
    VCON s:filum = accipere() <- ("name? >");
    indicant() <- (s);
    VCON n:inte = inte() <- (accipere() <- ("number? >"));
    indicant() <- (n*2);
};
</DOCTRINA>
